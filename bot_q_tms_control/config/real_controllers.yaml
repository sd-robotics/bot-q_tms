/**/controller_manager:
  ros__parameters:
    update_rate: 50  # Hz

    velocity_controller:
      type: velocity_controllers/JointGroupVelocityController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

/**/velocity_controller:
  ros__parameters:
    joints:
      - base_spool_joint
      - base_guider_joint
      - extruder_gear_joint
      # - extruder_gear_sub_joint
